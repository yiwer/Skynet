---
status: accepted
---

# 采集所有项目，并向已认证用户开放全部数据查看

平台首版用于内部自用。用户明确选择采集所有项目的全部会话，允许按项目区分展示；通过认证的用户可以查看全部数据，不设置按员工或团队划分的读取权限。因此首版不采用先前建议的工作项目白名单或“员工只看自己”方案，换取接入与使用上的简单性。

认证仍是访问前提，员工身份仍用于归属和汇总；查看开放不等于取消身份认证，也不意味着客户端可以冒充其他员工上传。写入、删除和恢复时如何处理原记录及归属，属于单独的操作语义，尚未被本决策授权为任意修改。
